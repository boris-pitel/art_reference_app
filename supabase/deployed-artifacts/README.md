# Deployed Edge Function artifacts

These `.eszip` files were downloaded from live Supabase project `bbcgcrbvxmertipdjczu` on 2026-08-01 using `supabase functions download`. They are the exact compiled artifacts returned by Supabase, including legacy deployed functions.

The CLI download operation returned compiled bundles. Editable TypeScript was subsequently extracted from the authenticated Dashboard code editors and is stored under `../functions/`. Keep these bundles as deployment evidence and disaster-recovery artifacts.

See `functions-manifest.json` for live version numbers, runtime hashes, JWT settings, and downloaded artifact hashes.
