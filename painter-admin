#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"
if [[ -f .env.admin ]]; then
  set -a
  # shellcheck disable=SC1091
  source .env.admin
  set +a
fi
exec dart run tool/painter_admin/painter_admin.dart "$@"