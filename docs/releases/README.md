# Release notes

Each deployed Painter Reference version has a Markdown file named
`<version>+<build>.md` in this folder.

The release workflow creates a template automatically after it increments the
build number. Before deployment is handed off, replace the template items with
the features, improvements, bug fixes, and deployment targets in that release.
