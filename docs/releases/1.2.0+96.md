# 1.2.0+96

- Continuous in-app camera capture: each shot is saved locally; uploads resume after leaving the camera. No per-shot Use Photo confirmation.
- Native queued originals stored as files; lightweight queue summaries and one-at-a-time upload reads. Existing queue records retained through migration.
- Dedicated upload queue view, compact category indicator, matching offline thumbnail and full-screen viewing controls.
- AI Edit remembers 32 recent prompts, adds Clear prompt and outline presets with thickness and palette/custom RGB color controls.
- Compare attached images against another attachment or the main reference.

Validation: Android debug compilation succeeded. Queue persistence and migration, offline gallery, editor, prompt-history, and thumbnail regression checks are run before publication. Physical camera testing remains required on Android/iPhone.

Targets: Android Firebase App Distribution, iPhone TestFlight, Windows. No changes to the current App Store review submission.
