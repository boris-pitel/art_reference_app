# Painter Reference 1.2.0+95

Prepared 2026-09-08.

- Offline photos appear in their category grid even when the category has no downloaded listing.
- Queued originals can be opened locally and remain visible when reopening the category.
- Saving to the offline queue does not request a server category refresh.
- Existing session restoration updates open screens and triggers pending upload synchronization.
- The reconnect sign-in action appears only when there is no authenticated session.

Deployment targets: iPhone TestFlight, Android Firebase App Distribution, local Windows app.
Validation: eight offline category/queue/session/connectivity tests passed; changed application files passed Dart analysis.
